/*
Grocery shopping
*/

/*
let arr=['Toothpaste','Brush','flour','Oil']
*/

for shoppingList.items[];
    {
        this.skuArray = [];
        this.SaveItem = [];
    };

    shoppingBasket.prototype.toNumber = function(value)
   {
       form.remove();
   } 
   ShoppingCart.prototype.checkoutCOD = function (parms, clearCart) {
   }
   function CartItem(sku, name, slug, mrp, price, quantity, image, category, packing) {
        this.sku = sku;
        this.name = name;
        this.slug = slug;
        this.image = image;
        this.category = category;
        this.packing = packing;
        this.mrp = mrp;
        this.price = price * 1;
        this.quantity = quantity * 1;
        this.status = 0;
    }

